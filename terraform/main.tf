provider "aws" { region = "eu-west-3" } variable "docker_image" { type = string default = "bgoussama/nextgen-app:latest" } resource "aws_security_group" "nextgen-sg" { name_prefix = "nextgen-sg-" description = "Allow inbound traffic on port 22 and 80" lifecycle { create_before_destroy = true } ingress { from_port = 22 to_port = 22 protocol = "tcp" cidr_blocks = ["0.0.0.0/0"] } ingress { from_port = 80 to_port = 80 protocol = "tcp" cidr_blocks = ["0.0.0.0/0"] } egress { from_port = 0 to_port = 0 protocol = "-1" cidr_blocks = ["0.0.0.0/0"] } tags = { Project = "PFS-2026" } } resource "aws_instance" "nextgen-ec2" { ami = "ami-011fc4a229f0661be" instance_type = "t3.micro" key_name = "nextgen-key" vpc_security_group_ids = [aws_security_group.nextgen-sg.id] user_data_replace_on_change = true user_data = "#!/bin/bash\nyum update -y\nyum install -y docker\nsystemctl start docker\nsystemctl enable docker\ndocker rm -f nextgen-app || true\ndocker pull ${var.docker_image}\ndocker run -d --name nextgen-app -p 80:80 --restart unless-stopped ${var.docker_image}" tags = { Project = "PFS-2026" } }